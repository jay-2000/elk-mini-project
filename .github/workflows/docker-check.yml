name: ELK Stack CI

on:
  push:
    branches:
      - main

jobs:
  docker-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Docker version
        run: docker --version

      - name: Docker Compose version
        run: docker compose version

